# Assignment-4
This repository includes the task 1 which opens and reads a file named sample.txt and prints its contents line by line. It also handles errors if the file doesn't exist. and 2 of assignment 4.
