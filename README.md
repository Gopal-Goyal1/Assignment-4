# Assignment-4
This repository includes the task 1 which opens and reads a file named sample.txt and prints its contents line by line. It also handles errors if the file doesn't exist. It also includes task 2 which overwrites a file named output.txt and also gives the option to append additional data in it and then finally prints its final content.
